# Demo 簡報講稿

預估時間：12-15 分鐘

---

## 第 1 頁 — 封面

> 大家好，今天要報告的是「台灣 OSM 動態路網導航系統」。這個系統是基於 OpenStreetMap 的台灣路網資料，結合交通部 TDX 即時車速資料和中央氣象署的氣象資料，透過多因子成本模型來做路徑規劃。

---

## 第 2 頁 — 研究動機與問題定義

> 現有的導航系統，像 Google Maps，它的路徑成本模型是不透明的，我們沒辦法知道它怎麼算出路線的，也沒辦法根據自己的需求調整。比如說一個怕走山路的使用者，和一個趕時間的使用者，需要的路線其實很不一樣。
>
> 另外一個問題是，這些系統沒有充分利用台灣本地的即時資料。TDX 交通資料平台有全台 1,361 個 VD 車速偵測器的即時數據，中央氣象署有 700 多個氣象站的即時天氣資料，這些都可以用來提升路線品質。
>
> 所以我的目標是建立一個可解釋的多因子成本模型，讓每個影響路線的因素都有學術研究的依據，而且使用者可以透過三種模式 — 最快、平衡、最安全 — 來選擇適合自己的路線。

---

## 第 3 頁 — 系統架構總覽

> 先講整體流程，讓大家知道從使用者點地圖到看到路線之間發生了什麼事。
>
> 系統分四層。最上層是前端，用 Leaflet.js 做的地圖介面。使用者在地圖上點兩個點，或是輸入地址搜尋，就會產生起點和終點的經緯度。
>
> 前端把這組經緯度透過 HTTP POST 傳到第二層 — FastAPI 的 REST API。API 收到座標後，先做輸入驗證，確認座標在台灣範圍內（lat 21.5~26.5, lon 118~122.5），然後呼叫第三層的路由引擎。
>
> 路由引擎是整個系統的核心。它先用 nearest_node 找到離起終點最近的路網節點 — 這一步很關鍵，因為使用者點的位置不一定剛好在路上，需要「吸附」到最近的道路節點。找到起終節點後，A* 演算法在記憶體中的圖上搜尋最佳路徑，每條邊的成本由多因子公式計算。
>
> A* 找到路徑後，結果是一串節點 ID 和邊的序列。每條邊都帶有經緯度（lat_a, lon_a → lat_b, lon_b），路由引擎把這些邊轉換成 GeoJSON 格式 — 這是地理資訊的標準格式，每條邊變成一條 LineString，帶有道路名稱、距離、速度等屬性。
>
> GeoJSON 回傳給前端後，Leaflet.js 直接用 L.geoJSON() 把它畫在地圖上。每條邊根據道路類型用不同顏色（國道藍、省道橘、縣道紫），底下還有一層深色描邊增加對比度。這就是你在地圖上看到的路線。
>
> 最底層是 SQLite 資料庫，存了 376 萬個節點和 765 萬條有向邊。路由引擎啟動時會把主幹路網預載到記憶體，所以 A* 搜尋不需要即時查資料庫。
>
> 右邊是兩個外部資料源。TDX 提供即時車速和路況新聞，CWA 提供氣象和雨量。系統每 5 分鐘自動同步，同步完後更新資料庫中每條邊的 dynamic_mult 和 risk_score，下一次路由查詢就會使用最新的成本。

---

## 第 4 頁 — 資料來源與規模

> 路網資料來自 OpenStreetMap 的台灣 PBF 檔。PBF 是 OSM 的二進位格式，台灣的檔案大約 309 MB。我們用離線工具 build_osm_graph.py 處理它，分兩次掃描：第一次掃描所有道路（Way），篩選有 highway 標籤的，收集道路類型、名稱、單行道等屬性，同時偵測號誌節點；第二次掃描節點座標。然後把每條道路展開成有向邊 — 雙向道路變成 A→B 和 B→A 兩條邊，單行道只有一條。
>
> 處理完產生 376 萬節點和 765 萬條有向邊。每條邊存了起終點經緯度、距離（haversine 公式計算）、道路類型和對應速度。整個建置大約 23 秒。
>
> 即時資料方面，TDX 的 VD 偵測器分布在全台 19 個縣市，主要在國道和快速道路上，共 1,361 站。每站回報即時車速，系統跟自由流速比較，低於 70% 就標記壅塞。CWA 有兩種站：自動氣象站約 700 多個，提供風速和能見度；雨量站約 400 多個，提供每小時降雨量。
>
> 另外，我們從 OSM 資料中解析出 56,026 個號誌節點。PBF 格式裡節點有兩種存法 — Regular Nodes 和 DenseNodes，兩種都要解析才能完整抓到所有標記為 highway=traffic_signals 的紅綠燈位置。

---

## 第 5 頁 — A* 搜尋演算法

> 路徑搜尋用的是 A* 演算法。啟發式函數用 haversine 距離除以 120 km/h，120 是台灣的最高速限，所以這個啟發式一定不會高估實際時間，滿足可容許性條件，保證找到的是最佳路徑。
>
> 搜尋有設定 200 萬步的上限，防止在斷開的子圖上無窮搜尋。
>
> 系統提供三種路線模式。fastest 把 risk_weight 設成 0.1，幾乎只看時間。balanced 是預設模式，risk_weight 0.4，在速度和安全之間取平衡。safest 把 risk_weight 拉到 0.9，會大幅避開高風險區域，即使要繞路。
>
> 另外還有替代路線功能，用 penalty-based 的方法，找到主路線後，把主路線上的邊成本加 60%，再搜一次就會找到不同的路線，最多提供 3 條。

---

## 第 6 頁 — ★ 核心成本公式（重點）

> 這是整個系統的核心。每條邊的成本由這個公式計算：
>
> **edge_cost = adj_time × (time_weight + risk × risk_weight) + turn_penalty + signal_delay**
>
> 拆開來看有六個因子：
>
> 第一個是 **adj_time**，基礎行車時間，這是距離除以速度再考慮時段因素。第二個是 **event_mult**，事件成本乘數，像事故會把時間乘以 1.8。第三個是 **weather_mult**，天氣會根據雨量、風速、能見度來增加成本。第四個是 **night_risk**，夜間行車的風險加成。第五個是 **turn_penalty**，轉彎的時間延遲。第六個是 **signal_delay**，每經過一個紅綠燈的等待時間。
>
> 這六個因子每一個都有學術研究支撐，接下來我逐一說明。

---

## 第 7 頁 — ★ 因子 1：基礎行車時間與時段速度

> 基礎行車時間的核心是道路速度模型。我們根據 OSM 的道路類型指派不同速度，國道 110、快速道路 90、省道 60，一路到住宅區 30 km/h。
>
> 但是光有速度還不夠，因為同一條路在不同時段的車速差很多。右邊這張表是時段速度乘數，可以看到早晚尖峰時段，國道還能維持 0.85，但省道就降到 0.70，鄉道更低到 0.62。也就是說，尖峰時段城市道路的有效速度會降低 30 到 40%。
>
> 這個設計是有研究依據的。TDVRP 的研究指出，考慮時間相依的路徑規劃可以消除 99% 的遲到情況。如果不考慮時段因素，系統在尖峰時段算出的路線會嚴重低估實際行程時間。

---

## 第 8 頁 — ★ 因子 2：事件成本乘數

> 事件成本乘數有 8 種類型。事故乘以 1.8，施工乘以 1.55，道路封閉就是 999 等於封路。壅塞的 1.35 是比較保守的值，因為壅塞的實際速度已經反映在 VD 偵測資料裡了。
>
> 壅塞偵測的原理是：當 VD 偵測到的即時車速低於該路段自由流速的 70%，就建立一個壅塞事件。severity 跟速度降低的比例成正比。70% 這個閾值在 FHWA 的定義中對應到服務等級 E 和 F，也就是嚴重壅塞的範圍。
>
> 比較特別的是山崩風險，這是針對台灣的地理特性設計的。台灣的山區道路在大雨時很容易發生山崩，所以我們根據 CWA 的累積雨量設了三個等級：200 毫米以上警戒、350 毫米高風險、600 毫米以上等效封路。這些閾值來自水保局的風險指數分級。

---

## 第 9 頁 — ★ 因子 3：天氣成本乘數（重點）

> 天氣成本是一個加權公式：1.0 加上 0.40 乘以雨量、0.20 乘以風速、0.35 乘以能見度、0.30 乘以警報等級。
>
> 權重的設計有學術依據。雨量權重最高 0.40，因為 HCM 的研究顯示降雨造成 2 到 17% 的速度降低，影響最顯著。風速權重最低 0.20，因為學術研究一致指出風對小客車速度的影響遠小於雨和能見度，通常被描述為「統計顯著但效果量較小」。能見度 0.35 是因為低能見度雖然降速幅度不是最大，但事故風險會大幅上升，在 safest 模式下影響會被 risk_weight 放大。
>
> 雨量的正規化用的是平方根函數，不是簡單的線性除法。原因是如果用線性，小雨 2.5 毫米只會增加 1.2% 的成本，但 Iowa State 的研究顯示小雨就會造成 3 到 5% 的降速。用平方根之後，同樣的小雨會增加約 7%，更符合研究數據。
>
> 風速的起算點設在 8 m/s，因為研究顯示低於 8 m/s 的風對一般小客車幾乎沒有影響。

---

## 第 10 頁 — ★ 因子 4：夜間行車風險

> 夜間風險是一個很重要但常被忽略的因子。NHTSA 的數據很驚人：夜間行車只占總里程的 25%，卻造成了 50% 的死亡事故。致死率是日間的 3 到 9 倍。
>
> 所以我們根據時段加入 risk_score 的加成。日間 7 點到 17 點是基線，不加成。黃昏加 0.05，夜間加 0.15，深夜 0 到 4 點加最多的 0.25，因為這個時段除了能見度差，還有疲勞駕駛的因素。
>
> 這個因子在不同模式下的影響差很多。fastest 模式的 risk_weight 只有 0.1，所以夜間風險幾乎不會改變路線。但在 safest 模式下 risk_weight 是 0.9，夜間規劃的路線就會明顯偏向照明好、車流多的主要道路，避開偏僻路段。

---

## 第 11 頁 — ★ 因子 5：轉彎延遲

> 轉彎延遲是城市路線中很容易被忽略的時間成本。McGill 大學的研究指出，轉彎延遲占城市行程時間的 15 到 25%。
>
> 實作方式是在 A* 搜尋展開鄰居節點時，追蹤前一條邊的方位角，然後計算跟下一條邊的角度差。角度差小於 20 度算直行，不加延遲。20 到 60 度是微轉，加 3 秒。60 到 130 度是正常左右轉，加 10 秒。急轉加 15 秒，迴轉加 25 秒。
>
> 這些數字取自 HCM 和 Transport Geography 的研究。加了轉彎延遲之後，系統算出的城市路線會比較偏好直行的幹道，減少不必要的轉彎，這跟實際駕駛的偏好是一致的。

---

## 第 12 頁 — ★ 因子 6：號誌密度延遲

> 最後一個成本因子是號誌延遲。每經過一個紅綠燈加 20 秒，這是 HCM 每號誌 15 到 45 秒延遲的中位數。高速公路和快速道路豁免，因為是封閉式道路沒有號誌。
>
> 我們從 OSM 資料中解析出全台 56,026 個號誌節點。台北市大約 5,600 個，高雄市大約 7,000 個。城市幹道每公里大約有 2 到 6 個號誌，所以每公里會多 40 到 120 秒。
>
> HCM 的研究指出，號誌延遲占城市行程時間的 20 到 40%。這也解釋了為什麼城市裡走高架道路明明距離比較遠，實際到達時間卻比較快 — 因為沒有紅綠燈。加了這個因子之後，系統在城市中會更傾向選擇號誌較少的路線。

---

## 第 13 頁 — 即時資料同步架構

> 即時資料每 300 秒同步一次，流程是：先清除上一輪的即時資料，然後依序擷取 TDX 車速、路況新聞、CWA 氣象和雨量，偵測山崩風險，最後重算全部 765 萬條邊的動態成本。
>
> 有一個設計要特別提的是來源分離。即時資料標記為 realtime，每次同步會全部清除重建。但管理員手動建立的事件標記為 manual，不會被同步影響。這樣管理員可以手動新增一個施工事件，即使同步了好幾輪，那個事件還是會在。

---

## 第 14 頁 — GPS 即時導航功能

> 系統支援完整的 GPS 導航功能。計算完路線後按下開始導航，就會出現轉彎指示的 HUD，顯示下一個轉彎的方向、距離和目前道路名。
>
> 偏離路線偵測的閾值是 80 公尺，但需要連續 3 次才觸發，避免 GPS 雜訊造成的假陽性。觸發後會自動從目前位置重新規劃到原本的目的地。
>
> 另外每 60 秒會重新載入事件圖層，如果路線上出現新的事件，也會自動重新規劃，避免開進剛發生事故的路段。

---

## 第 15 頁 — 安全設計

> 安全方面主要防三種攻擊。
>
> 第一個是 **SQL 注入**。簡單說就是防止有人在輸入欄裡塞 SQL 指令來偷或改資料庫內容。我們的做法是所有資料庫查詢都用問號佔位符，不會把使用者輸入直接拼進 SQL 裡面。另外像道路類型這種欄位有白名單，不在清單裡的值直接拒絕。
>
> 第二個是 **XSS**，就是防止有人在搜尋框或事件描述裡塞 JavaScript 程式碼。前端所有從後端拿到的文字，在顯示之前都會先把特殊符號轉成安全的文字，這樣即使有人故意輸入程式碼也不會被執行。
>
> 第三個是 **認證**。系統分兩種使用者：一般使用者只能看地圖、規劃路線；管理員才能新增事件、觸發同步這些操作。從本機 localhost 存取的話自動當管理員，從外面連進來就需要提供 token。token 的比對用了防時序攻擊的方式，不會因為猜對前幾個字就回應比較快。
>
> 另外就是錯誤訊息不會洩漏系統內部資訊，使用者只會看到「路徑計算失敗」這種通用訊息，不會看到程式碼的錯誤細節。多人同時用的時候有加鎖機制，不會互相干擾。

---

## 第 16 頁 — 效能最佳化

> 765 萬條邊的路網全部載入記憶體會很吃資源，所以做了幾個最佳化。
>
> **省記憶體**：每條邊用 namedtuple 存，比用 Python 字典省了大約 60% 的記憶體。差別在於字典每一條都要存欄位名稱，namedtuple 只存值，一百多萬條邊加起來就差了幾百 MB。
>
> **分層載入**：系統不會一次把 765 萬條邊全部載入記憶體，而是只載國道到鄉道等級的主幹路網，大約 134 萬條。巷弄等級的路只有在短程路線（15 公里以內）或起終點附近才會從資料庫臨時載入。這樣長途路線計算完全在記憶體裡面跑，不用等資料庫。
>
> **局部更新**：每次同步完要重算路網成本，但不是 765 萬條邊全部重算，而是只更新被事件或天氣影響到的那一小塊區域的邊。
>
> **快取**：VD 偵測器的位置資料只需要從 TDX 抓一次就存起來，之後同步直接用快取。地址搜尋結果也會暫存 5 分鐘，同一個地址不用重複呼叫外部 API。
>
> 實際跑起來的數據：啟動載入大約 10 秒，台北到台中 170 公里的路由計算不到 1 秒，一輪完整同步（抓 TDX + CWA + 重算成本）大約 2 分鐘。

---

## 第 17 頁 — 現場 Demo

> 接下來做現場展示。
>
> *(按照投影片上的 7 步流程操作，展示地圖、搜尋、路線規劃、模式切換、圖層、導航、手動事件)*

---

## 第 18 頁 — 結論與未來工作

> 總結一下，這個系統實作了 8 項成本因子，每一項都有學術研究的支撐。整合了 TDX 和 CWA 的即時資料，每 5 分鐘自動同步。提供完整的 GPS 導航功能，包含轉彎指示、偏離偵測和事件觸發重路由。還有替代路線功能，最多提供 3 條不同的路線。
>
> 未來的工作方向包括：加入道路坡度因子，需要 SRTM 的高程資料；BPR 模型需要累積 VD 歷史流量數據；路面品質看公路局是否開放 IRI 資料；淹水風險可以整合水利署的即時資料。

---

## 第 19 頁 — 參考文獻

> 最後這頁是所有引用的參考文獻，包括 FHWA 的 Highway Capacity Manual、NHTSA 的夜間行車研究、McGill 大學的轉彎延遲研究，以及台灣水保局的山崩風險分級標準等 16 篇文獻。
>
> 以上就是我的報告，謝謝大家。

---

## 附錄：常見提問準備

### Q: 為什麼不直接用 Google Maps API？
> Google Maps 的成本模型是黑盒子，無法自訂也無法解釋。本系統的每個成本因子都是透明的，可以調整權重，也有學術依據。另外 Google Maps API 有呼叫次數和費用限制。

### Q: 70% 自由流速的壅塞閾值依據是什麼？
> FHWA 定義的服務等級 E 和 F（嚴重壅塞）大約在自由流速 60~70% 以下。我們取 70% 作為觸發閾值，比較靈敏地偵測壅塞開始的時間點。

### Q: 為什麼雨量用 sqrt 而不是線性？
> 線性正規化（除以 80mm）會嚴重低估小雨的影響。2.5mm/h 的小雨，線性只有 +1.2%，但 Iowa State 研究顯示實際降速 3~5%。sqrt 函數讓小雨區間的斜率更陡，更貼合研究數據。大雨區間兩者趨近一致。

### Q: 夜間風險會不會造成夜間路線不合理地長？
> 不會，因為它只加在 risk 分量上。fastest 模式的 risk_weight 只有 0.1，夜間 +0.25 的加成換算下來對總成本影響不到 3%。只有在 safest 模式下（risk_weight=0.9）才會顯著影響路線選擇。

### Q: 轉彎延遲會不會讓系統都走直線、避免所有轉彎？
> 不會，因為延遲值是依據研究設定的合理範圍。直行 0 秒、普通轉彎 10 秒，這些相對於路段行車時間來說不是非常大。系統只會在兩條路線時間差不多的時候，偏好轉彎比較少的那條。

### Q: 號誌延遲 20 秒是平均值嗎？
> 是 HCM 給出的 15~45 秒範圍的中位數。實際上號誌延遲取決於週期長度、綠燈比例和到達時間，要精確計算需要每個路口的號誌時制資料，這超出了目前的資料範圍。20 秒作為統計平均值是合理的估計。

### Q: 系統能處理多少同時路由請求？
> 路由引擎有 RLock 保護記憶體圖，同時的路由請求會序列化處理。SQLite 用 WAL 模式支援讀寫並行。實務上單機可以處理每秒約 1~2 個路由請求，足夠 Demo 和小規模使用。

### Q: 替代路線的品質如何保證？
> Penalty-based 方法的原理是把主路線的邊加 60% 懲罰，迫使 A* 搜尋不同的路徑。60% 是經過測試選定的值，太低會找到幾乎一樣的路，太高會找到不合理的繞路。實測台北市區 3 條路線距離差異在 20~30%，長途則在 10~20%。
